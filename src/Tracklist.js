import './App.css';


// request data from spotify, json response contains a set of song tracks
// jamming app should display the song name artist and album for each track in the results list
// create a unidirectional data flow from the root component
// the root component should pass down the search results to a child component that will
// return each individual track to be rendered

export default App;
